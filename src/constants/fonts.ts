// Font variable class names - CSS variables are defined in globals.css
// Google Fonts are loaded via @import in globals.css (works on Vercel)
// System font fallbacks are used when fonts are unavailable

export const heading = { variable: "font-heading" };

export const base = { variable: "font-base" };

export const handwriting = { variable: "font-handwriting" };

export const display = { variable: "font-display" };
